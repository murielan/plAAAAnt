<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.NETWORK" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/EmobaTheme">
        <activity
            android:name="fhnw.ws6c.MainActivity"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/EmobaTheme.NoActionBar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <service
            android:name=".theapp.data.MqttService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />
        <receiver
            android:name=".theapp.receiver.MqttStaticReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="fhnw.ws6c.ACTION_NEW_MESSAGE" />
                <action android:name="fhnw.ws6c.ACTION_CONNECTION_FAILED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>